// some header

// this is a godoc. // want `package has more than one godoc \("multiple"\)`
//
// Note that the "2|4" in the pattern is because the analyzer will process the
// package twice; with and without Go test files.
package multiple
