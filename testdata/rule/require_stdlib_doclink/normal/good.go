// godoc with doclink to [encoding/json.Encoder]
package normal

// godoc with doclink to [encoding/json.Encoder]
const Alpha = 0

// godoc with doclink to [encoding/json.Encoder.Encode]
const Bravo = 0

// godoc with doclink to [*encoding/json.Encoder]
const Charlie = 0

// godoc with doclink to [encoding/json.Encoder] and doclink to [bytes.Buffer].
const Delta = 0

// godoc with doclink to [encoding/json.Encoder] and [*encoding/json.Encoder] and
// doclink to [bytes.Buffer] and [*bytes.Buffer].
const Echo = 0

// godoc with doclink to [io.PipeWriter.Close]
const Golf = 0

// godoc with doclink to [io.PipeWriter.Close] and doclink to [bytes.Buffer].
const Foxtrot = 0

// godoc with doclink to [io.PipeWriter.Close] and [bytes.Buffer] and [*bytes.Buffer].
const Hotel = 0

// godoc with common words that look like potential doclinks but they are not.
//
// 8 bits are bytes
//
// fmt is a package
//
// the encoding/json package is cool
//
// there's no such thing as encoding/json.Play (yet?)
const Juliet = 0

// godoc with wrong potential doclinks to non-existing symbols, so they should just be ignored.
//
// fmt.PRINTLN
//
// encoding/json.encoder
//
// io.PipeWriter.Closer
const Kilo = 0
