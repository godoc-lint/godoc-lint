// some header

// this file has no issues under the default configuration

// package has no godoc and it's okay since `require-pkg-doc` is disabled by default

package default_config

// symbol has no godoc and it's okay since `require-doc` is disabled by default

const FooNG = 0

// symbol has long godoc and it's okay since `max-len` is disabled by default

// Bar is a const
// ??????????????????????????????????????????????????????????????????????????????
const Bar = 0

// Baz is a const with an unused link
//
// [link]: https://foo.com
const Baz = 0

// Yolo godoc has a potential doc link to encoding/json.Encoder, but it's okay.
const Yolo = 0
